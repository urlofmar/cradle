name: GCC

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

env:
  CTEST_OUTPUT_ON_FAILURE: 1

jobs:

  gcc-10:
    runs-on: ubuntu-20.04

    strategy:
      matrix:
        config: [Debug, Release]

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Cache
      uses: actions/cache@v2
      with:
        path: |
          ~/.conan/data
        key: ${{runner.os}}-gcc-${{matrix.config}}

    - name: Install
      run: |
        sudo apt install gcc-10 lcov ocaml-nox
        sudo pip3 install virtualenv

    - name: Configure
      run: |
        export CC=`which gcc-10`
        export CXX=`which g++-10`
        scripts/set-up-python.sh --python=python3
        source .venv/bin/activate
        scripts/set-up-conan.sh
        cmake -Bbuild -DCMAKE_BUILD_TYPE=${{matrix.config}} .

    - name: Build & Test
      run: >
        cmake --build build --config ${{matrix.config}}
        --target unit_test_coverage -j4

        python python/generate_config.py $CRADLE_THINKNODE_API_TOKEN

        cmake --build build --config ${{matrix.config}}
        --target integration_tests -j4
